# Interactive-Image-Segmentation-using-Mask-R-CNN-GrabCut
This project implements an image segmentation pipeline that combines deep learning-based instance segmentation with interactive refinement.  The system uses Mask R-CNN to detect and segment objects automatically, while GrabCut improves object boundaries through user-guided refinement.
